<svg width="80" height="80" viewBox="0 0 80 80" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <linearGradient id="badgeGrad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" style="stop-color:#E63946;stop-opacity:1" />
      <stop offset="50%" style="stop-color:#F4A261;stop-opacity:1" />
      <stop offset="100%" style="stop-color:#E76F51;stop-opacity:1" />
    </linearGradient>
  </defs>

  <!-- Japanese character background -->
  <text x="40" y="35"
        font-family="'Noto Sans JP', sans-serif"
        font-size="60"
        font-weight="700"
        fill="#E63946"
        opacity="0.1"
        text-anchor="middle">開</text>

  <!-- Outer circle -->
  <circle cx="40" cy="40" r="36"
          fill="none"
          stroke="url(#badgeGrad)"
          stroke-width="2.5"/>

  <!-- Inner filled circle -->
  <circle cx="40" cy="40" r="28"
          fill="url(#badgeGrad)"/>

  <!-- RKS text -->
  <text x="40" y="47"
        font-family="'Zen Kaku Gothic New', sans-serif"
        font-size="20"
        font-weight="700"
        fill="#FFFFFF"
        text-anchor="middle"
        letter-spacing="1.5">RKS</text>

  <!-- Top accent dot -->
  <circle cx="40" cy="6" r="2" fill="url(#badgeGrad)"/>

  <!-- Bottom accent dot -->
  <circle cx="40" cy="74" r="2" fill="url(#badgeGrad)"/>
</svg>
